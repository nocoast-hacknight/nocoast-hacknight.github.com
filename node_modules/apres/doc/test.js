
define(
  ['apres', 'jquery'],
  function(apres, $) {
    var controller = {};

    controller.events = {
      "widgetReady #markdown": function(ev, widget) {
        widget.src(apres.queryParams.p);
      },
    };

    return controller;
  }
);
